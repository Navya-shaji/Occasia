import fs from 'fs';
import path from 'path';
import nodemailer from 'nodemailer';
import dotenv from 'dotenv';

dotenv.config();

const transporter = nodemailer.createTransport({
  service: 'gmail',
  auth: {
    user: process.env.EMAIL_USER,
    pass: process.env.EMAIL_PASS,
  },
});

export const sendOtpEmail = async (email: string, otp: string) => {
  // Logic already exists...
  console.log(`|  CODE:    ${otp}                          |`);
  // ... rest of the code is simplified here for brevity during write_to_file if needed, 
  // but I should probably just append or use replace_file_content.
};

// I'll use replace_file_content instead to preserve the complex logging logic.
